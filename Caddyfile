polarispall.com, www.polarispall.com {
    encode gzip

    # Serve media files directly
    handle_path /media/* {
        root * /media
        file_server
    }

    # Proxy everything else to the web container
    handle {
        reverse_proxy web:8000
    }
}
